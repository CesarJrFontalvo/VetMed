import styled from 'styled-components';
import { Button, Navbar } from 'react-bootstrap';

export const Barranav = styled(Navbar)`
    background: #63CEC1;
`;

export const ContainerDiv = styled.div`
    padding-top: 58px;
`;

export const Buttomnew = styled(Button)`
    margin-left: 10px;
`;